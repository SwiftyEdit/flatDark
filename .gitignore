# OS
.DS_Store
Thumbs.db

# IDEs / editors
.idea/
.vscode/
*.iml

# Node (dependencies only - dist/ is the built theme and stays tracked,
# since the installed theme is used as-is without a build step)
/node_modules/
npm-debug.log*

# Local dev notes (not part of the published theme)
/SESSION-NOTES.md
/todo.md

# Release build artifacts (attach to GitHub Releases instead of committing)
/*.zip

# Logs
*.log
